fields:
    websockets_server_ip:
        label: Websockets Server IP
    websockets_server_port:
        label: Websockets Server Port
    require_user:
        label: Require user
        type: switch
    subscriptions_limit:
        label: Subscriptions limit
        type: number